import { Layout } from '@/components/layout/Layout';
import { Landing } from './Landing';

const Index = () => {
  return (
    <Layout>
      <Landing />
    </Layout>
  );
};

export default Index;
